import { createDB } from "./createDB.mjs";

const key = "run build";

if (key === "run build") {
  createDB();
} else {
  console.log("else");
  // const returnString =
  //   key +
  //   " = " +
  //   acronyms.find((o) => o.acronym.toLowerCase() === key.toLowerCase()).value +
  //   "for more info click";
  // //, '<a href={"https://www.google.com"}> here </a>'
}

//return acronyms.find((o) => o.acronym === key).value;
